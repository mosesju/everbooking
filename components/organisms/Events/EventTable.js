import SimpleTable from '../../molecules/Table/SimpleTable'

export default function EventTable () {
    // Need a table molecule
    // needs to connect to Supabase and get event based on user ID
    return (
        <div>
            <SimpleTable />
        </div>
    )
}